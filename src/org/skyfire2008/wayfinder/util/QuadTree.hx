package org.skyfire2008.wayfinder.util;

// import org.skyfire2008.wayfinder.path.Region;
class QuadNode {
	/*public var maxObjects(default, null): Int;
		public var level(default, null): Int;

		private var objects: Array<Region>;
		private var children: Array<QuadNode>;

		public function new(level: Int, maxObjects: Int) {
			this.level = level;
			this.maxObjects = maxObjects;
		}

		public function addObject(object: Region) {
			//if object quota not reached, or node is at lowest level, just add to object array
			if (objects.length < maxObjects || level==0) {
				objects.push(object);
			} else {
				//first determine whether object is too big for children
				//
			}
	}*/
}
